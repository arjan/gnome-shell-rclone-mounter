<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.rclone-mounter"
          path="/org/gnome/shell/extensions/rclone-mounter/">
    <key name="mountpoints" type="a{ss}">
      <default>{}</default>
      <summary>Configured mounts</summary>
      <description>
        Maps an rclone remote name to the directory it is mounted on. Only
        remotes listed here appear in the panel menu.
      </description>
    </key>
    <key name="mount-at-login" type="as">
      <default>[]</default>
      <summary>Mount at login</summary>
      <description>
        Remote names to mount automatically when the extension starts.
      </description>
    </key>
    <key name="mount-options" type="s">
      <default>'--vfs-cache-mode=writes'</default>
      <summary>Extra rclone mount flags</summary>
      <description>
        Additional command line flags appended to every rclone mount call.
      </description>
    </key>
  </schema>
</schemalist>
