# Fixture for the parser tests, not a usable rclone configuration.

[gdrive-work]
type = drive
scope = drive
team_drive = 0ABCDEF

; semicolon comments are legal in rclone.conf too
[  s3-backups  ]
type = s3
provider = Wasabi

[crypted]
type   =   crypt
remote = s3-backups:vault
